<li>
	<%= link_to user.name, user %>
	<% if current_user.admin? %>
	<%= button_to "Delete User", user, :method => :delete, :confirm => "Are you sure?", :title => "Delete #{user.name}" %>
	<% end %>
</li>

